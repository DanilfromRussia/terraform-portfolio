terraform {
  required_providers {
    yandex = {
      source = "yandex-cloud/yandex"
    }
  }
}

resource "yandex_vpc_security_group" "sg-emelyanov2002" {
        name = "sg-emelyanov2002-cloudname"
        network_id = var.network_id

        dynamic "ingress" {
        for_each = var.allowed_ports
                content {
                        protocol = "TCP"
                        description = "rule1 description"
                        v4_cidr_blocks = ["0.0.0.0/0"]
                        port = ingress.value
                }
        }
}